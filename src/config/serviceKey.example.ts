/**
 * serviceKey.ts 템플릿.
 *
 * 이 파일을 같은 폴더에 `serviceKey.ts` 로 복사한 뒤 발급받은 키를 채우세요.
 * `serviceKey.ts` 는 .gitignore 에 등록돼 있어 커밋되지 않습니다.
 *
 * 키 발급: https://www.data.go.kr 에서 아래 두 서비스를 활용신청
 * - 국토교통부_(TAGO)_버스정류소정보 (데이터 15098534)
 * - 국토교통부_(TAGO)_버스도착정보   (데이터 15098530)
 *
 * 마이페이지 > 데이터활용 > Open API > 활용신청 현황 에서
 * **일반 인증키(Decoding)** 를 복사해 넣습니다. Encoding 키가 아닙니다.
 */
export const SERVICE_KEY = '';
